<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_home">

    <fragment
        android:id="@+id/nav_home"
        android:name="com.example.blogs_app.ui.logout.home"
        android:label="@string/home"
        tools:layout="@layout/home" />

    <fragment
        android:id="@+id/nav_gallery"
        android:name="com.example.blogs_app.ui.logout.profile"
        android:label="@string/profile"
        tools:layout="@layout/profile" />

    <fragment
        android:id="@+id/nav_slideshow"
        android:name="com.example.blogs_app.ui.logout.sitting"
        android:label="@string/sitting"
        tools:layout="@layout/sitting" />
    
</navigation>